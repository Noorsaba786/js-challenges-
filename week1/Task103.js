// creqte a function that print the multiplication table of a given number up to 10

let number = 5; // we  can change this number to get the multiplication table of any number

for (let i = 1; i <= 10; i++) {
    let result = number * i;
    console.log(number + " x " + i + " = " + result);
}
